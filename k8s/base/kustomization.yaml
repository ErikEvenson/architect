apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmaps.yaml
  # namespace.yaml — applied separately (contains multiple namespaces)
  # secrets-template.yaml — reference only, use scripts/create-secrets.sh
  # migration-job.yaml — applied separately before services
