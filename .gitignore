# Runtime data (externalized to PVC)
/data/

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.venv/
*.egg

# Node
node_modules/
.npm
*.tsbuildinfo

# Test artifacts
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage/
test.db
*.sqlite3

# TLS certificates
*.pem
*.key
*.crt
*.p12
*.pfx
*.jks

# Secrets and credentials — NEVER commit these
.env
.env.*
secrets.yaml
architect-secrets.yaml
credentials.json
*-credentials.json
*.credentials
service-account*.json
kubeconfig
kubeconfig.yaml
*-kubeconfig.yaml
token
*.token

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Docker
*.tar

# Agent workspace
.claude/

# Sibling repo cloned in-tree for local MCP work (has its own remote)
architect-knowledge-mcp/

# Confidential client-name patterns for the git hooks (see .example)
.githooks/client-patterns.local

# mkdocs build output — `mkdocs build` writes ~400 generated files here, and a
# `git add -A` after a local docs preview commits every one of them.
site/
