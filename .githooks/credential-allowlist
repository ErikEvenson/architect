# Accepted credential-scan findings, keyed by `<path>:<sha256 of the matched line>`.
# A line hash rather than a whole-file exemption: editing the line re-flags it, so
# this cannot silently cover a real key added to the same file later.
#
# Versioned on purpose — an accepted finding is by definition not a secret.
# Add an entry ONLY with a comment saying why it is safe.
#
# Documentation placeholder in the knowledge library; `prod-password` is illustrative
# Pulumi config, not a value. (knowledge/providers/pulumi/iac.md)
knowledge/providers/pulumi/iac.md:d3036ac304303c69401901ec297a5794cb07855fa8ee1747dbd4774c495a8279
